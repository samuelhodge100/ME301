# Odometry based on velocity values

def Odometry(robot_position, encoder_speeds,wheel_dirs,dt):
    # encoder_speeds is an array of the encoder speeds
    # encoder_dir is an array of motor directions
    

    return 